---
# Icecast Relay role handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
  become: yes

- name: Restart relays
  ansible.builtin.systemd:
    name: "{{ relay_service_prefix }}{{ item.id }}"
    state: restarted
  loop: "{{ relay_channels }}"
  become: yes
