---
# Liquidsoap role handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
  become: yes

- name: Restart liquidsoap
  ansible.builtin.systemd:
    name: "{{ liquidsoap_service_name }}"
    state: restarted
  become: yes
