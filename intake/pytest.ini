[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    integration: requires MHBN_TEST_DATABASE_URL pointing at a real postgres
    e2e: requires the headroom test-pair pipeline (loki + headroom WG-reachable)
